# 
version: 2.1

orbs:
  aws-cli: circleci/aws-cli@5.1.1

jobs:
  test-oidc:
    docker:
      - image: cimg/base:stable  # lightweight; includes curl, jq, etc.
    steps:
      - run:
          name: 🔍 Decode CircleCI OIDC Token
          command: |
            echo "🔍 Checking for CircleCI OIDC token..."
            if [ -n "$CIRCLE_OIDC_TOKEN_V2" ]; then
              TOKEN="$CIRCLE_OIDC_TOKEN_V2"
            elif [ -n "$CIRCLE_OIDC_TOKEN_FILE" ]; then
              TOKEN=$(cat "$CIRCLE_OIDC_TOKEN_FILE")
            else
              echo "❌ No OIDC token found."
              exit 1
            fi

            echo ""
            echo "🔍 Decoding token payload..."
            echo "$TOKEN" | cut -d '.' -f2 | tr '_-' '/+' | base64 --decode --ignore-garbage | jq '{aud, sub, iss}' || true

            echo ""
            echo "✅ Token detected and decoded successfully."

      - aws-cli/setup:
          role_arn: "arn:aws:iam::056198491146:role/CircleCIInfraRole"
          role_session_name: "circleci-oidc-test"
          region: "ap-southeast-1"
          profile_name: "oidc-test"
          session_duration: "3600"

      - run:
          name: 🧠 Verify AWS Identity
          command: |
            echo "🔍 Calling STS get-caller-identity..."
            aws sts get-caller-identity --profile oidc-test || {
              echo "❌ Failed to assume role."
              exit 1
            }

workflows:
  verify-oidc:
    jobs:
      - test-oidc
