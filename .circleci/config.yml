version: 2.1

orbs:
  aws-cli: circleci/aws-cli@5.1.1

jobs:
  test-oidc:
    docker:
      - image: cimg/base:stable

    steps:
      - checkout

      - run:
          name: 🔧 Environment info
          command: |
            echo "ORG ID:      $CIRCLE_ORGANIZATION_ID"
            echo "PROJECT ID:  $CIRCLE_PROJECT_ID"
            echo "USER:        $CIRCLE_USERNAME"
            echo "BRANCH:      $CIRCLE_BRANCH"
            echo "--------------------------------------"

      # 1️⃣ Fetch token from environment variables (CircleCI provides it automatically)
      - run:
          name: 🔑 Find CircleCI OIDC token
          command: |
            set -e
            echo "🔍 Checking for OIDC token..."

            if [ -n "$CIRCLE_OIDC_TOKEN_V2" ]; then
              echo "✅ Found CIRCLE_OIDC_TOKEN_V2"
              TOKEN="$CIRCLE_OIDC_TOKEN_V2"
            elif [ -n "$CIRCLE_OIDC_TOKEN_FILE" ]; then
              echo "✅ Found CIRCLE_OIDC_TOKEN_FILE"
              TOKEN=$(cat "$CIRCLE_OIDC_TOKEN_FILE")
            elif [ -n "$CIRCLE_OIDC_TOKEN" ]; then
              echo "✅ Found legacy CIRCLE_OIDC_TOKEN"
              TOKEN="$CIRCLE_OIDC_TOKEN"
            else
              echo "❌ No OIDC token found. Make sure your context has OIDC enabled."
              exit 1
            fi

            echo "$TOKEN" > token.jwt

      # 2️⃣ Decode and show key claims
      - run:
          name: 🧠 Decode OIDC token
          command: |
            echo "--------------------------------------"
            echo "🧠 Decoding iss, aud, sub claims..."
            jq -R 'split(".")[1] | @base64d | fromjson | {iss,aud,sub}' token.jwt
            echo "--------------------------------------"
            echo "📜 Full payload (for debug only):"
            jq -R 'split(".")[1] | @base64d | fromjson' token.jwt
            echo "--------------------------------------"

      # 3️⃣ Configure AWS CLI with OIDC (official orb)
      - aws-cli/setup:
          role-arn: "arn:aws:iam::056198491146:role/CircleCIInfraRole"
          role-session-name: "circleci-oidc-test"
          region: "ap-southeast-1"
          profile-name: "oidc-test"
          session-duration: "3600"

      # 4️⃣ Verify AWS credentials after orb setup
      - run:
          name: 🧠 Verify AWS Identity (via orb)
          command: |
            echo "🔍 Verifying assumed AWS identity..."
            aws sts get-caller-identity --profile oidc-test --region ap-southeast-1 || {
              echo "❌ Failed to assume role via OIDC."
              exit 1
            }
            echo "✅ Successfully assumed AWS role via CircleCI OIDC!"

workflows:
  verify-oidc:
    jobs:
      - test-oidc:
          context: my-oidc-enabled-context
